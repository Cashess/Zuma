[build]
  command = "prisma generate && next build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-prisma"

[build.environment]
  NODE_ENV = "production"
  NEXT_PUBLIC_API_URL = "https://zumashop.netlify.app"
