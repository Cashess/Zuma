[build]
  command = "prisma migrate deploy && prisma generate && next build"


[[plugins]]
  package = "@netlify/plugin-prisma"

[build.environment]
  NODE_ENV = "production"
  NEXT_PUBLIC_API_URL = "https://zumashop.netlify.app"
